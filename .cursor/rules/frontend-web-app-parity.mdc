---
description: 前端改动默认同时评估 Flutter Web 与 App
alwaysApply: true
---

# Frontend Web/App 一致性规则

## 适用范围
- 所有前端需求、交互、状态流转与展示逻辑修改。

## 执行要求
- 默认同时检查 Flutter Web 与 App 的行为一致性。
- 若同一问题在两端都存在，应同步修复并保证交互语义一致。
- 若某一端已正确无问题，不做冗余修改，只修另一端。
- 在结论中明确说明：Web/App 各自是否改动，以及不改动的理由。

## 目标
- 避免“只修一端”导致体验分叉，同时避免无效改动。
