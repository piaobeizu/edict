[pytest]
asyncio_mode = auto
testpaths = tests kernel/tests
markers =
    e2e: end-to-end tests covering full workflow command pipeline
    integration: integration tests with mocked dependencies
    unit: pure unit tests
